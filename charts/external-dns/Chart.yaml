apiVersion: v2
name: external-dns-wrapper
description: External DNS wrapper chart with extended RBAC templates
type: application
version: 1.19.0
appVersion: "0.19.0"

# This is a wrapper chart that includes:
# 1. The official external-dns chart as a dependency
# 2. Custom extended RBAC templates

dependencies:
  - name: external-dns
    version: 1.19.0
    repository: "file://./charts"
    condition: external-dns.enabled

keywords:
  - external-dns
  - dns
  - route53
  - aws

maintainers:
  - name: Platform Team

annotations:
  category: Infrastructure
