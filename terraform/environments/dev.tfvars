# Development environment configuration

environment = "dev"
enable_pod_identity = true

tags = {
  Terraform   = "true"
  Environment = "dev"
  Project     = "eks-addons"
  Owner       = "platform-team"
}